name: singlecells_analysis

dependencies:
  - conda-forge::python

  - pip:
      - bokeh==3.3.4
      - numpy
      - matplotlib
      - jupyterlab
      - nd2
      - dask          # lazy per-position nd2 reads (process_lazy / run_server_stream)
      - xarray        # nd2.to_xarray(delayed=True)

